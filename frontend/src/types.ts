export type QuestionType = {
    name: string
    label: string
};

export type AnswerType = {
    question: string
    favourite: string
    worst: string
    underrated: string
    friday: string
    sunday: string
};